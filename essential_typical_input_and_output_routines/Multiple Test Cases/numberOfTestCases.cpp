#include <bits/stdc++.h>

using namespace std;

int main() {
    int T; cin >> T;
    while (T--) {
        int a, b; // cin >> a >> b;
        scanf("%d %d", &a, &b);
        printf("%d\n", a+b);
    }

    return 0;
}